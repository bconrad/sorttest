Makes a list of random numbers and runs various sorts on them

TODO:
 - Accept arguments
 - Implement radix sort (I'm embarrassed about the current broken code)
 - See if there's an alternative to that damn macro
 - Optional graphical output (will require some clever Makefile'ing)
